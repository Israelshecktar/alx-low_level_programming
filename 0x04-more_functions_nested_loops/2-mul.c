#include "main.h"


/**
 * mul - this function multiplies two ints
 * @a: - is the first integer
 * @b: -  are to be checked too
 * Return: output Always success
*/

int mul(int a, int b)
{
	int output = a * b;

return (output);
}
